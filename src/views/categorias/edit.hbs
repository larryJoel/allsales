<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card">
                <div class="card-body">
                    <form action="/categorias/edit/{{links.id}}" method="POST">
                        <div class="form-group">
                            <input type="text" class="form-control" name="name" value="{{links.name}}" placeholder="Nombre de la Categoria" autofocus required>
                        </div>

                        <div class="form-group">
                            <textarea rows="2" class="form-control" name="descripcion"
                                placeholder="Descripción de la categoria">{{links.descripcion}}</textarea>
                        </div>
                        <div class="form-group">
                            <button class="btn btn-success btn-block">
                                Editar/Edit
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>